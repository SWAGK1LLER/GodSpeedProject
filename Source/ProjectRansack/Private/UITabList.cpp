#include "UITabList.h"

void UUITabList::HandleTabCreation(FName TabNameID, UCommonButtonBase* TabButton)
{
	//content->AddChild(TabButton);
}

void HandleTabRemoval(FName TabNameID, UCommonButtonBase* TabButton)
{
	//content->RemoveChild(TabButton);
}